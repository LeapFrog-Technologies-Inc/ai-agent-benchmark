services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: ai-agent-benchmark:latest
    container_name: ai-agent-benchmark
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
